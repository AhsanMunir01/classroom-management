export type Subject = {
    id: string;
    name: string;
    code: string;
    department: string;
    semester: string;
    credits: number;
    instructor: string;
    createdAt: string;
    updatedAt: string;
}
